import DirectedGraph from "../directedGraph";

describe("DirectedGraph", () => {
	let dg: DirectedGraph;

	it("gets created successfully", () => {
		dg = new DirectedGraph();
	});

	it("assigns edge directions", () => {

	});

	it("has the same number of edges", () => {

	});
});

